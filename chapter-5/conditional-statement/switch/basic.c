// switch statement basic
#include <stdio.h>
int main() {
    int num;
    printf("Enter a number: ");
    scanf("%d", &num);

    switch(condition){
        case 0:
            // code to be executed if condition is 1
            break;
        case 1:
            // code to be executed if condition is 2
            break;
        default:
            // code to be executed if condition is neither 1 nor 2
    }
    return 0;
}